const validationEnabled = {
  control: 'boolean',
  defaultValue: true
}
const required = {
  control: 'boolean'
}
const pattern = {
  control: 'text'
}
const minlength = {
  control: 'number'
}
const maxlength = {
  control: 'number'
}
const min = {
  control: 'number'
}
const max = {
  control: 'number'
}
const customMessages = {
  control: 'object'
}
const rules = {
  control: 'array'
}
const colorValid = {
  control: 'color'
}
const colorInvalid = {
  control: 'color'
}

export {
  validationEnabled,
  required,
  pattern,
  minlength,
  maxlength,
  min,
  max,
  customMessages,
  rules,
  colorValid,
  colorInvalid
}
export default {
  validationEnabled,
  required,
  pattern,
  minlength,
  maxlength,
  min,
  max,
  customMessages,
  rules,
  colorValid,
  colorInvalid
}
