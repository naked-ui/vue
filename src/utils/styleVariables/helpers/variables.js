// Size

const width = {
  name: 'width',
  type: 'size'
}

const height = {
  name: 'height',
  type: 'size'
}

const lineHeight = {
  name: 'lineHeight',
  type: 'size'
}

const gap = {
  name: 'gap',
  type: 'size'
}

const outlineWidth = {
  name: 'outlineWidth',
  type: 'size'
}

const padding = {
  name: 'padding',
  type: 'size'
}

const borderRadius = {
  name: 'borderRadius',
  type: 'size'
}

const borderWidth = {
  name: 'borderWidth',
  type: 'size'
}

const size = {
  name: 'size',
  type: 'size'
}

const fontSize = {
  name: 'fontSize',
  type: 'size'
}

const spacing = {
  name: 'spacing',
  type: 'size'
}

const itemsGap = {
  name: 'itemsGap',
  type: 'size'
}

const viewportPadding = {
  name: 'viewportPadding',
  type: 'size'
}

// Color

const color = {
  name: 'color',
  type: 'color'
}

const backgroundColor = {
  name: 'backgroundColor',
  type: 'color'
}

const borderColor = {
  name: 'borderColor',
  type: 'color'
}

// Other

const background = {
  name: 'background',
  type: 'other'
}

const border = {
  name: 'border',
  type: 'other'
}

const borderStyle = {
  name: 'borderStyle',
  type: 'other'
}

const resize = {
  name: 'resize',
  type: 'other'
}

export {
  width,
  height,
  gap,
  outlineWidth,
  padding,
  border,
  borderWidth,
  borderStyle,
  borderColor,
  borderRadius,
  size,
  fontSize,
  color,
  background,
  backgroundColor,
  spacing,
  itemsGap,
  viewportPadding,
  resize,
  lineHeight,
}
