// Size
const width = {
    name: 'width',
    type: 'size'
}

const height = {
    name: 'height',
    type: 'size'
}

const lineHeight = {
    name: 'lineHeight',
    type: 'size'
}

const gap = {
    name: 'gap',
    type: 'size'
}

const outlineWidth = {
    name: 'outlineWidth',
    type: 'size'
}

const padding = {
    name: 'padding',
    type: 'size'
}

const borderRadius = {
    name: 'borderRadius',
    type: 'size'
}

const size = {
    name: 'size',
    type: 'size'
}

const fontSize = {
    name: 'fontSize',
    type: 'size'
}

const spacing = {
    name: 'spacing',
    type: 'size'
}

const itemsGap = {
    name: 'itemsGap',
    type: 'size'
}

const viewportPadding = {
    name: 'viewportPadding',
    type: 'size'
}

const resize = {
    name: 'resize',
    type: 'size'
}

// Color
const colorValid = {
    name: 'colorValid',
    type: 'color'
}

const colorInvalid = {
    name: 'colorInvalid',
    type: 'color'
}

const color = {
    name: 'color',
    type: 'color'
}

const background = {
    name: 'background',
    type: 'other'
}

const backgroundColor = {
    name: 'backgroundColor',
    type: 'color'
}

const textColor = {
    name: 'textColor',
    type: 'color'
}

const borderColor = {
    name: 'borderColor',
    type: 'color'
}

export {
    width,
    height,
    gap,
    outlineWidth,
    padding,
    colorValid,
    colorInvalid,
    borderRadius,
    size,
    fontSize,
    color,
    background,
    backgroundColor,
    textColor,
    spacing,
    itemsGap,
    viewportPadding,
    resize,
    borderColor,
    lineHeight
}
