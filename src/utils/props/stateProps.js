const busy = {
  type: Boolean,
  default: false
}
const checked = {
  type: Boolean,
  default: false
}
const disabled = {
  type: Boolean,
  default: false
}
const readonly = {
  type: Boolean,
  default: false
}

export { busy, checked, disabled, readonly }
export default { busy, checked, disabled, readonly }
